# Guides

Use these when setting up or running normal development flows.

- `run-play.md`: Train/play commands, task IDs, naming/resume flags, and debug dump controls.
- `docker-setup.md`: Build and launch Docker environments for local and server runs.
